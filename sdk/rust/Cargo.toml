[package]
name = "cryptooffload-sdk"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = "1"
prost = "0.13"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
tonic = "0.12"
tracing = "0.1"

[build-dependencies]
tonic-build = "0.12"
